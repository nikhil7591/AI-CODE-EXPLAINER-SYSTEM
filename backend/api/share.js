const express = require('express');
const router = express.Router();

// Placeholder for share route
router.post('/', (req, res) => {
  res.status(501).json({ error: 'Not implemented yet' });
});

module.exports = router;
